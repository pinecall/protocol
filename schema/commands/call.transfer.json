{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pinecall.io/protocol/commands/call.transfer.json",
  "title": "call.transfer",
  "description": "Send the caller to another number. call.transferred says whether it worked.",
  "x-pinecall": {"kind": "command", "type": "call.transfer", "scope": "call", "produces": ["call.transferred"]},
  "$ref": "#/$defs/CallTransfer",
  "$defs": {
    "CallTransfer": {
      "type": "object",
      "description": "Send the caller to another number. call.transferred says whether it worked.",
      "properties": {
        "to": {"type": "string", "description": "The destination number in E.164 form, or a SIP URI."},
        "mode": {"$ref": "../defs.json#/$defs/TransferMode"}
      },
      "required": ["to", "mode"],
      "additionalProperties": false
    }
  }
}
