{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pinecall.io/protocol/commands/prompt.set.json",
  "title": "prompt.set",
  "description": "Rewrite one block of the prompt, whole, by name. The name must be one of the agent's declared blocks, or one of the default four; anything else is refused with the name.",
  "x-pinecall": {"kind": "command", "type": "prompt.set", "scope": "call", "produces": ["prompt.changed"]},
  "$ref": "#/$defs/PromptSet",
  "$defs": {
    "PromptSet": {
      "type": "object",
      "description": "Rewrite one block of the prompt, whole, by name. The name must be one of the agent's declared blocks, or one of the default four; anything else is refused with the name.",
      "properties": {
        "name": {"type": "string", "description": "The block to rewrite, by the name its PromptBlockSpec declares."},
        "text": {"type": "string", "description": "The block's new text, whole."}
      },
      "required": ["name", "text"],
      "additionalProperties": false
    }
  }
}
