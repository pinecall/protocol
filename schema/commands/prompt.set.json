{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pinecall.io/protocol/commands/prompt.set.json",
  "title": "prompt.set",
  "description": "Rewrite one region of the prompt. The static prefix is cached by the provider and rarely changes; the view is render(state) and changes with the state.",
  "x-pinecall": {"kind": "command", "type": "prompt.set", "scope": "call", "produces": ["prompt.changed"]},
  "$ref": "#/$defs/PromptSet",
  "$defs": {
    "PromptSet": {
      "type": "object",
      "description": "Rewrite one region of the prompt. The static prefix is cached by the provider and rarely changes; the view is render(state) and changes with the state.",
      "properties": {
        "region": {"$ref": "../defs.json#/$defs/PromptRegion"},
        "text": {"type": "string", "description": "The region's new text, whole."}
      },
      "required": ["region", "text"],
      "additionalProperties": false
    }
  }
}
