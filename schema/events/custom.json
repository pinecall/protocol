{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pinecall.io/protocol/events/custom.json",
  "title": "custom",
  "description": "A line the app wrote into the log with call.log. The platform never reads it; the console shows it and evals may.",
  "x-pinecall": {"kind": "event", "type": "custom", "scope": "call", "ephemeral": false},
  "$ref": "#/$defs/Custom",
  "$defs": {
    "Custom": {
      "type": "object",
      "description": "A line the app wrote into the log with call.log. The platform never reads it; the console shows it and evals may.",
      "properties": {
        "name": {"type": "string", "description": "The app's name for the line: slot_search, handoff_note."},
        "data": {"type": "object", "additionalProperties": true, "description": "Whatever the app attached."}
      },
      "required": ["name", "data"],
      "additionalProperties": false
    }
  }
}
