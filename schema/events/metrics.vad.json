{"$schema": "https://json-schema.org/draft/2020-12/schema", "$id": "https://pinecall.io/protocol/events/metrics.vad.json", "title": "metrics.vad", "description": "The VAD's health for the last second or so: livekit's VADMetrics, every field. About one a second, so ephemeral by default: the console sees it live, the store need not keep it.", "x-pinecall": {"kind": "event", "type": "metrics.vad", "scope": "call", "ephemeral": true}, "$ref": "../metrics.json#/$defs/VADMetrics"}
