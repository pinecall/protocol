{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pinecall.io/protocol/events/prompt.changed.json",
  "title": "prompt.changed",
  "description": "A block of the prompt was rewritten. The text stays out of the log; its hash and length let two states be compared.",
  "x-pinecall": {"kind": "event", "type": "prompt.changed", "scope": "call", "ephemeral": false},
  "$ref": "#/$defs/PromptChanged",
  "$defs": {
    "PromptChanged": {
      "type": "object",
      "description": "A block of the prompt was rewritten. The text stays out of the log; its hash and length let two states be compared.",
      "properties": {
        "name": {"type": "string", "description": "The block that was rewritten, by name."},
        "hash": {"type": "string", "description": "sha256 of the block's new text, hex."},
        "chars": {"type": "integer", "description": "The new text's length in characters."}
      },
      "required": ["name", "hash", "chars"],
      "additionalProperties": false
    }
  }
}
