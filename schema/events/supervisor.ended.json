{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pinecall.io/protocol/events/supervisor.ended.json",
  "title": "supervisor.ended",
  "description": "A supervisor hung up the call. call.ended follows with reason supervisor_ended.",
  "x-pinecall": {"kind": "event", "type": "supervisor.ended", "scope": "call", "ephemeral": false},
  "$ref": "#/$defs/SupervisorEnded",
  "$defs": {
    "SupervisorEnded": {
      "type": "object",
      "description": "A supervisor hung up the call. call.ended follows with reason supervisor_ended.",
      "properties": {
        "by": {"$ref": "../defs.json#/$defs/Supervisor"},
        "reason": {"type": "string", "description": "Why, in their words."}
      },
      "required": ["by"],
      "additionalProperties": false
    }
  }
}
