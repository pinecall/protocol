{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pinecall.io/protocol/events/supervisor.said.json",
  "title": "supervisor.said",
  "description": "A supervisor made the agent say this to the caller.",
  "x-pinecall": {"kind": "event", "type": "supervisor.said", "scope": "call", "ephemeral": false},
  "$ref": "#/$defs/SupervisorSaid",
  "$defs": {
    "SupervisorSaid": {
      "type": "object",
      "description": "A supervisor made the agent say this to the caller.",
      "properties": {
        "by": {"$ref": "../defs.json#/$defs/Supervisor"},
        "text": {"type": "string", "description": "What was said, word for word."}
      },
      "required": ["by", "text"],
      "additionalProperties": false
    }
  }
}
